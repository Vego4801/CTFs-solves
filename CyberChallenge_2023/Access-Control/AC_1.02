Find an executable file owned by flag00 with Set User ID.
Read the flag.txt file in flag00's home.

Log in as level00 with password level00.


Solution: Use the command "find / -type f -user flag00 -perm /4000 2>/dev/null" to find the executable
		  Run the executable which gives a flag to open the file (/home/flag00/flag.txt)s.

flag: CCIT{997874cf-581a-49ca-a449-51d24dd31fa5}
